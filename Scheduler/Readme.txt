To compile the program-

either execute the following command (to run using the makefile):
$ make scheduler

or execute the following command (without using makefile):
$ g++ -o scheduler scheduler.cpp


Usage:
$ ./scheduler -s<FLS|R<num>|P<num>> <input_file_along_with_path> <random_file_along_with_path>